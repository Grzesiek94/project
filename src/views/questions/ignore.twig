{# Delete action template for Users controller #}
{% extends 'base.twig' %}

{% block title %}{{  'Ignore question'|trans }}{% endblock %}

{% block head %}
     {{ parent() }}
{% endblock %}

{% block content %}
    <h1>
        {{ 'Ignore question'|trans }}
    </h1>
    <form action="{{ url('ignore', {'id':id}) }}" method="post">
        {{ form_widget(form) }}
	<p>Jesteś pewien, że chcesz zignorowac to pytanie?</p>
        <input type="submit" class="btn btn-success" name="submit" value="{{ 'Yes'|trans }}" />
	<a href="{{ url('questions_index') }}" class="btn btn-danger" >{{ 'NOPE!'|trans }}</a>
    </form>
{% endblock %}
