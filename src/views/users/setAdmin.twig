{# Delete action template for Users controller #}
{% extends 'base.twig' %}

{% block title %}{{  'set new admin'|trans }}{% endblock %}

{% block head %}
     {{ parent() }}
{% endblock %}

{% block content %}
    <h1>
        {{ 'Are You sure?'|trans }}
    </h1>
    <form action="{{ url('set_admin', {'id':id}) }}" method="post">
        {{ form_widget(form) }}
	<p>Jesteś pewien, że chcesz nadac prawa administratora wybranemu uzytkownikowi?</p>
        <input type="submit" class="btn btn-success" name="submit" value="{{ 'Yes'|trans }}" />
	<a href="{{ url('user_index') }}" class="btn btn-danger" >{{ 'NOPE!'|trans }}</a>
    </form>
{% endblock %}
